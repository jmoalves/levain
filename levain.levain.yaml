version: 0.0.1

baseDir: ${levainHome}/levain

cmd.install:
  - mkdir --compact ${levainHome} 
  - mkdir ${baseDir} 
  - copy src ${baseDir}
  - copy bin ${baseDir}
  - copy levain.cmd ${baseDir}
  - copy levain.sh ${baseDir}
  - addpath ${baseDir}/bin
