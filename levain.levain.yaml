version: 0.0.1

cmd.install:
  - copy --verbose src bin levain.cmd levain.sh ${baseDir}
  - saveConfig
  - levainShell ${pkgDir}\src\userPath.cmd ${baseDir}\bin ${baseDir}

cmd.shell:
  - addPath ${baseDir}
