version: 0.50.0

cmd.install:
  - copy --verbose ../levain.bundle.js ../bin ../extra-bin ../levain.cmd ../levain.sh ../userPath.cmd ../recipes ${baseDir}
  - addPath --permanent ${baseDir}/bin
  - addPath --permanent ${baseDir}
  # Tab completion
  # Command extensions
  # UNC Path
  - levainShell reg import ${pkgDir}/cmd-config.reg

cmd.env:
  - addPath ${baseDir}
