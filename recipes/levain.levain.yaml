version: 0.86.9-SNAPSHOT

cmd.install:
  - copy --verbose ../levain.cmd ../levain.sh ../build ${baseDir}
  - addPath --permanent ${baseDir}/bin
  - addPath --permanent ${baseDir}
  # Tab completion
  # Command extensions
  # UNC Path
  - levainShell reg import ${pkgDir}/cmd-config.reg 2>nul

cmd.env:
  - addPath ${baseDir}
